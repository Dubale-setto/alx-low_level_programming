#include "main.h"
/**
  * print_alphabet_x10 - prints the alphabet 10 times
  *
  *Return always 0 sucess
  */
void print_alphabet_x10(void)
{
	char ch;
	int i;

	i = 0;
	while (i < 0)
	{
		ch = 'a';
		while (ch < 'z')
		{
			_putchar(ch);
			ch++;
		}
	_putchar('\n');
		i++;
	}
}
