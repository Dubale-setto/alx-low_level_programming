#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This program prints the last digit of a random number.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	har c;
for (c = 'a'; c <= 'z'; ++c)
	putchar(c);
for (c = 'A'; c <= 'Z'; ++c)
	putchar(c);
	putchar('\n');
return (0);
}
